import 'package:flutter/material.dart';

class CapricornScreen extends StatelessWidget {
  const CapricornScreen({super.key, sign});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
       child: Text('Hello'),
      ),
    );
  }
}
